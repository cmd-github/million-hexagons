import {chromium} from 'playwright';
const base=(process.env.SMOKE_URL||'http://127.0.0.1:4180').replace(/\/$/,'');
import assert from 'node:assert/strict';
import fs from 'node:fs';
const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});
const errors=[];fs.mkdirSync('artifacts/globe-design',{recursive:true});
try{
for(const mobile of [false,true]){
 const page=await browser.newPage({viewport:mobile?{width:390,height:844}:{width:1440,height:900},isMobile:mobile,hasTouch:mobile});
 page.setDefaultTimeout(20000);page.on('pageerror',e=>errors.push(e.message));
 await page.goto(base+'/?geodesicQA');await page.waitForSelector('#world[data-ready=true]',{timeout:60000});
 const createDistance=await page.evaluate(()=>Math.hypot(...window.geodesicQA.state().camera));await page.locator('#claimButton').click();await page.locator('#designStep').waitFor();assert.ok(Math.abs(Math.hypot(...await page.evaluate(()=>window.geodesicQA.state().camera))-createDistance)<.001,'Create must preserve the current globe zoom');
 assert.equal(await page.locator('#canvasSurface').count(),0);
 assert.equal(await page.locator('.size-menu').count(),0);
 assert.equal(await page.locator('#paintCells').getAttribute('aria-pressed'),'true');
 assert.equal(await page.locator('#sizeControls>span').first().textContent(),'Hexagons');assert.equal(await page.locator('#placeStep').isHidden(),true);assert.equal(await page.locator('.hex-brush').count(),4);assert.match(await page.locator('.hex-brush').first().evaluate(element=>getComputedStyle(element.querySelector('i')).clipPath),/polygon/);
 const clickCell=async id=>{const p=await page.evaluate(id=>window.geodesicQA.screen(id),id);if(mobile)await page.touchscreen.tap(p.x,p.y);else await page.mouse.click(p.x,p.y);};
 const initial=await page.evaluate(()=>window.geodesicQA.state().design);
 assert.deepEqual(initial,[]);assert.equal(await page.locator('#toPlacement').isDisabled(),true);assert.equal(await page.locator('#addHexagon').isDisabled(),true);
 await page.locator('[data-brush="1"]').click();const first=await page.evaluate(()=>window.geodesicQA.state().designAnchor);await clickCell(first);await page.waitForFunction(()=>window.geodesicQA.state().design.length===7);assert.equal(await page.locator('#toPlacement').isEnabled(),true);
 const startingCount=Number(await page.locator('#hexAmount').inputValue());await page.locator('#addHexagon').click();await page.waitForFunction(count=>Number(document.querySelector('#hexAmount').value)===count+1,startingCount);await page.locator('#removeHexagon').click();await page.waitForFunction(count=>Number(document.querySelector('#hexAmount').value)===count,startingCount);
 await page.screenshot({path:'artifacts/globe-design/'+mobile+'-draw.png'});
 await page.locator('[data-brush="2"]').click();const neighbour=await page.evaluate(id=>window.geodesicQA.neighbours(id)[0],first);await clickCell(neighbour);await page.waitForFunction(()=>window.geodesicQA.state().design.length>7);
 const ids=await page.evaluate(()=>window.geodesicQA.state().design);
 await clickCell(first);await page.locator('#undoPaint').click();
 await page.locator('#removeHexMode').click();await page.locator('[data-brush="0"]').click();await clickCell(ids.at(-1));
 await page.waitForFunction(n=>window.geodesicQA.state().design.length<n,ids.length);
 await page.locator('#undoPaint').click();
 await page.locator('#logoUpload').setInputFiles('scripts/fixtures/test-logo.svg');
 await page.waitForFunction(()=>document.querySelector('#addImageLabel').textContent==='Change image');
 await page.locator('#moveImageMode').click();await page.locator('#logoOrientation').fill('37');await page.locator('#logoScale').fill('140');
 await page.screenshot({path:'artifacts/globe-design/'+mobile+'-globe.png'});
 await page.locator('#toPlacement').click();assert.deepEqual(await page.evaluate(()=>window.geodesicQA.state().selected),ids);
 await page.locator('#reviewEditDesign').click();assert.equal(await page.locator('#logoOrientation').inputValue(),'37');
 await page.locator('#toPlacement').click();
 await page.locator('#website').fill('javascript:alert(1)');await page.locator('#previewPurchase').click();assert.equal(await page.locator('#websiteError').isVisible(),true);
 await page.locator('#website').fill('https://example.com/');
 await page.locator('#companyName').fill('Orbit Studio');await page.locator('#companyDescription').fill('Independent design for curious people.');
 await page.locator('#previewPurchase').click();await page.waitForFunction(()=>document.querySelector('#buyPanel').getAttribute('aria-hidden')==='true',null,{timeout:60000});
 const committed=await page.evaluate(()=>window.geodesicQA.state().committed);assert.ok(ids.every(id=>committed.includes(id)));
 await page.locator('#claimButton').click();await page.locator('#designStep').waitFor({state:'visible'});
 await page.locator('.studio-more summary').click();await page.locator('#startOver').click();
 assert.equal(await page.locator('#addImageLabel').textContent(),'Add image');
 assert.equal(await page.locator('#logoOrientation').inputValue(),'0');
 await page.locator('#closeBuy').click();
 await page.locator('#toggleHexSearch').click();await page.locator('#hexSearchInput').fill('Orbit');await page.locator('#companyResults button').first().click();
 await page.locator('#placementInspector').waitFor({state:'visible'});
 assert.equal(await page.locator('#inspectorNearby').count(),0);
 assert.equal(await page.locator('#inspectorName').textContent(),'Orbit Studio');
 assert.equal(await page.locator('#inspectorLogo').isVisible(),true);assert.ok(await page.locator('#inspectorLogo').evaluate(e=>e.complete&&e.naturalWidth>0));
 assert.equal(await page.locator('#inspectorDescription').textContent(),'Independent design for curious people.');
 assert.match(await page.locator('#inspectorDate').textContent(),/\d{2}\/\d{2}\/\d{2}/);
 assert.doesNotMatch(await page.locator('#claimFeedItems').textContent(),/Orbit Studio/);assert.match(await page.locator('#claimFeedItems').textContent(),/next live placement/i);
 assert.equal(await page.locator('#discoverPlacement').count(),0);
 await page.locator('#pinInspector').click();await page.mouse.click(8,100);assert.equal(await page.locator('#placementInspector').isVisible(),true);
 await page.locator('#pinInspector').click();await page.mouse.click(8,100);assert.equal(await page.locator('#placementInspector').isVisible(),false);
 await page.locator('#toggleHexSearch').click();await page.locator('#hexSearchInput').fill('Orbit');await page.locator('#companyResults button').first().click();
 await page.waitForTimeout(1500);await page.screenshot({path:'artifacts/globe-design/'+mobile+'-inspect.png'});
 await page.close();
}
assert.deepEqual(errors,[]);console.log('Globe-only desktop/mobile brush, undo, rotation, source reset, review parity and HUD checks passed');
}finally{await browser.close();}
