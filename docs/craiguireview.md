Activity Log
- [x] on the activity log - show x days / hours / minutes ago not dates
- [x] start activity log expanded
- [x] newly placed items should auto update the activity log - not require refresh
UI
- [x] improve colour on background 'star' hexs - not brighter but closer to the brand colour
- [x] try introducing some white as well as green on bg 'star' hexs
- [x] rotate the globe faster
- [x] when auto-spinning on zoomed in the globe should spin slower (i.e. globe speed depends on zoom level)
- [x] globe tour should maintain full logo / placement in view when going to a target and maintain some slow globe movement on zoom in to keep it feeeling alive
Menu
- [x] "Sign-in link sent. Open it on any device; you may be asked to confirm this email." just sign-in link sent. check spam if not received
- [x] search bar should come out next to search icon, not above it.
- [x] seacrh bar allows searching with blank form - it shouldn't show any error if clicked when blank.

Placements
- [x] create a placemnet button jumps on globe to super zoomed in - clicking that shouldn't move teh globe yet
- [x] redesign the placement UX
- [x] paint, add and remove image should be combined - clicking the hex's on the globe in the placement view should claim them in the colour selected - images can then be added on top (satrt on that option)
- [x] allow multiple images per placement (up to five layers)
- [x] paint brush sizes dont step sensibly - use a size picker like small (1 hex) medium large and xl
- [x] three button menu on placement make no sense - image options should go on the image tab
- [x] image should be moveable as well as zoomable - ie to move within the choosen area
- [x] "Place" / Find my Spot paeg makes no sense - especially as the spot has just been designed on the map it should be combined into the make it yours step
- [x] "Review" stage - need to be able to change user/ signout
- [x] "Company Name" "Company Description" it wont always be a company - could be individuals
- [x] "server confirmed" remove wording
- [x] placement should be reserved for 20 mins - alert user when time running down - subtle - good UI. allow I need more time - to add 10mins to the countdown but not indefinitely.
- [x] dont require https on website - allow even "xxxx.com" for example - make sure site is not 404
- [x] if user already logged in can email be prefilled on stripe form
- [x] on payment recevied screen - and adding placement to globe - make the text larger and use loading animation
- [x] Create starts with "1. Choose your starting spot" and keeps the globe movable; selection is only available once individual hexagons are visible
- [x] clicking an available starting hexagon should propose it first, then require an explicit Start here confirmation in the normal cell popup
- [x] Choose another spot should repeat that confirmation and move the complete design rather than rebuilding or losing it
- [x] once a paint colour has actually been used, add it to a recent colours pane for quick reuse
- [x] Review should not show generic Stripe email copy; show the signed-in email and Log out only when a user is signed in
- [x] call the artwork card Preview rather than colour placement
- [x] make the 160-character description field tall enough to show a full entry
- [x] expand and shuffle the hero's changing words, type and delete the full stop with each word, and hold completed words for one extra second
